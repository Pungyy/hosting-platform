# Token partagé Agent <-> Traefik.
# DOIT être identique à AGENT_TOKEN de apps/agent/.env : Traefik le présente
# en Bearer à l'Agent quand il récupère la configuration dynamique.
AGENT_TOKEN=dev_agent_change_me_to_a_long_random_string
